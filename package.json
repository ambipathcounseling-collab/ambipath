{
  "name": "ambipath-fullstack",
  "version": "1.0.0",
  "scripts": {
    "server": "cd backend && nodemon server.js",
    "client": "npx live-server frontend --port=5500",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}